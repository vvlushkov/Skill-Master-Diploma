spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/skill-master
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
  security:
    user.name: test
    user.password: test